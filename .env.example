# Example environment configuration
# Copy this file to .env for local development

# Application Environment
APP_ENV=development
DEBUG=true

# Development Mode
# Set to "true" to enable dev mode (bypasses authentication, uses local files)
DEV_MODE=true

# Note: Sensitive credentials should be stored in .streamlit/secrets.toml
# This file is for non-sensitive environment variables only
